
TODO:
- crear test suite que corra los scripts en orden

  ssh ---> sftp

- no enviar archivo al home, sino que al backup_folder/test
- revisar dump.sh y loaddata.sh, en busca de cosas testeables

- carpetas y archivos "writables" o "readables"
if [ -r "$FILE" ]; then
	echo "File is readable"
fi
if [ -w "$FILE" ]; then
	echo "File or Folder is writable"
fi



cat ~/.ssh/id_rsa.pub >> ~/.ssh/authorized_keys
ssh mpavez@localhost -i .ssh/id_rsa




tests to be done
===============================================

ON (TRANSAPP)
- requirements are installed:
	- ifconfig
	- tar
	- ...

- ssh connection to real viz server
- sftp connection to real server
- permisions


ON (TRANSAPP VIZ)
- permisions
- ...




