
TODO:
- instrucciones sobre requerimientos para correr los tests
.. ej: key existe, key es autorizada a si mismo, key ya fue usada por primera vez y agregada como util.
	cat ~/.ssh/id_rsa.pub >> ~/.ssh/authorized_keys
	ssh mpavez@localhost -i .ssh/id_rsa

	# en el mismo server
	sudo mkdir -p bkps
	sudo chown -R server bkps
	
	sudo -u root python manage.py test -k AndroidRequestsBackups

	# testing
	ANDROID_REQUESTS_BACKUPS_THIS_USER_TEST      = "server"
	ANDROID_REQUESTS_BACKUPS_THIS_USER_HOME_TEST = "/home/server"


tests:
- ejecucion por root
- correr loaddata.sh partial
- correr loaddata.sh complete


# todo: check dumps y vars solo en server
# todo: check loads y vars solo en remote